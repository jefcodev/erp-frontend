export class Apu {
    constructor(
        public id: number,
        public date_apu: string,
        public meters: number,
        public place: string,
        public percentage: number,
        public created: string,
        public total: number,
    ) {
    }
}